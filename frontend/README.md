# Learnify Frontend (Vite + React + Tailwind)

This is a small starter scaffold for the online learning frontend.

Quick start (Windows PowerShell):

```powershell
cd "f:\Bò iu Study\IT4409-LMS\frontend_test"
npm install
npm run dev
```

Notes:
- Uses Vite + React (JavaScript) and TailwindCSS.
- Global font is set to `Lexend` in `src/index.css`.
- Routes: `/login` and `/register` (App.jsx). The `AuthPage` component converts the original `Login - Register.html` into React components.
